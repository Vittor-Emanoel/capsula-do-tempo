/* eslint-disable prettier/prettier */
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // eslint-disable-next-line prettier/prettier
    domains: ["avatars.githubusercontent.com", "192.168.3.181"],
  },
};

module.exports = nextConfig;
